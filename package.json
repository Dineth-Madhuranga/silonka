{
  "name": "silonka-fullstack",
  "version": "1.0.0",
  "description": "Heroku deployment root for Silonka",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm install --prefix backend && npm install --prefix app && npm run build --prefix app"
  }
}
